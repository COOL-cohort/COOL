package com.nus.cool.core.field;

public interface HashField extends FieldValue {
  // should move hash logic here.
  // WS value impl then use corresponding FieldValue type instead of int and String.
}
